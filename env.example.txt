# Copy this to .env and fill in. .env is blocked by .gitignore — never commit it.

# ---- Option A: testing now, before the admin authorizes the app -------------
# Developer Console -> your app -> Developer Token -> Generate.
# Acts as YOUR account. Valid ~60 minutes. Revoke when finished.
BOX_DEVELOPER_TOKEN=

# ---- Option B: the real path, once the app is Authorized -------------------
# Configuration tab -> OAuth 2.0 Credentials (2FA needed to view the secret)
# General Settings -> Enterprise ID
BOX_CLIENT_ID=
BOX_CLIENT_SECRET=
BOX_ENTERPRISE_ID=

# ---- Which folder to crawl --------------------------------------------------
# 318353711369 = TomHanratty          (the accession — start here)
# 318345592147 = File_Archiving Project (the parent — everything, later)
BOX_ROOT_FOLDER_ID=318353711369
